# Functions on documents library

Import:

    from doc_fns import (
        page_subset,
        paste_translated_pages,
        writer_from_page_iterable,
    )

Installation:

    pip install git+https://github.com/phantie/doc_fns.git